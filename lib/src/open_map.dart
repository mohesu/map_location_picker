import 'package:flutter/material.dart';

class OpenMapLocationPicker extends StatelessWidget {
  const OpenMapLocationPicker({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
